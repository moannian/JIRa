import React, { useEffect } from "react"
import { Form } from "antd"

interface Iprops<D> {
    data?: D
}
const CustomForm: React.FC<{}> = React.memo(() => {
    return (
        <Form>

        </Form>
    )
})