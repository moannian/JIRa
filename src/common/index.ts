export type Row = string | number;
export interface Iproject {
    pin: boolean,
    id: number,
    project: string,
    department: string,
    principal: string,
    createTime: string
    projectID: number
}